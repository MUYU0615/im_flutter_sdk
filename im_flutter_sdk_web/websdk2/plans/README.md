# plans 目录说明

- `plans/active/`：仅放跨 feature 的进行中计划
- `plans/archive/`：放已完成或仅需留档的历史计划

使用约定：

- 新功能的正式技术方案优先写进 `specs/<feature-id>/plan.md`
- 只有不适合落入单个 `specs/*/plan.md` 的跨功能协作计划，才写进 `plans/active/`
- 计划执行完后，迁移到 `plans/archive/`
